Heart Disease Prediction using AI/ML

Steps to Run:
1. Install requirements: pip install -r requirements.txt
2. Train the model: python model.py
3. Run the web app: streamlit run app.py

Input your medical values and get a real-time prediction.
